let x: number = 1; // Basic typescript annotation ( : number )
// First ts code is converted into js code then it is executed.
// To convert ts code into js code, we use tsc command. ( npx tsc -b ) -> It will build js folder for paticular ts file.
console.log(x);

// Helps us catch error in compile time.